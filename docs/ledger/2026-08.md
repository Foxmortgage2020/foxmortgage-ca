# Session ledger — August 2026

Dated narrative notes, one file per month. Current state lives in CLAUDE.md;
this file is history.

### 2026-08-01 — Deals (Beta): the four-phase board over the `rec` record layer

Design inputs, read from `fox-underwriting` and NOT modified:
`docs/design/four-phase-model-handoff.md` (the model, section 5) and
`docs/design/command-centre-dashboard.html` (a layout argument — its structural
claims were kept, and none of its counts, names or figures were carried across).

- **Route** `/portal/admin/deals-beta`, gated on the existing `deals.view`. Nav
  item `Deals (Beta)` in the `book` group directly beneath `Deals`. The live
  Deals area is untouched — `git status` over
  `app/portal/admin/underwriting`, `components/admin/deals` and
  `lib/deals-surface.ts` is clean.
- **Read-only by construction, not by rule.** A SERVER component: phase
  selection rides `searchParams` through plain links, so the page ships no
  client JavaScript, and there is no form, handler or drag target in the tree.
  Reads go through `lib/underwriting.ts`, whose whole surface is a GET as
  `portal_readonly`. **Live write probe: `POST rec.deals` answers 403 /
  42501 "permission denied for table deals."** No service role key was added.
- **`rec` is reached by `Accept-Profile`**, a read-side header added as an
  optional argument to the existing `uwFetch`. Its write-side twin,
  `Content-Profile`, is never sent because this module never writes.
- **Stages are configuration.** Columns come from `rec.deal_stages` where
  `phase` is not null, ordered by `sort_order`, read at runtime. No stage list
  exists in code; a test proves adding a stage row adds a column.
- **The lime.** Only the `You` blocked-by chip takes the decision token. It is
  registered as a ninth surface in the `tests/shell.test.ts` audit with a test
  asserting Client, Lender and Lawyer stay in the cool family — the You chip
  only means something because the others are quiet.

**THE DAYS-IN-STAGE RULE, and where the brief met data it did not predict.**
The brief anticipated one gap: two of seven deals have no stage history, so
they show no figure. The live data has a SECOND gap it did not name.
`BRXM-F057400` and `BRXM-F025547` both sit in `lender_response`, and both carry
exactly one event — entry into `submitted`. Nothing records their entry into
the stage they are actually in. Taking the latest event of any kind would print
"72 days" under a `lender_response` heading, which is a fabricated figure
wearing a real one's clothes. So the rule implemented is narrower than the
brief's wording: **a figure renders only when an event's `to_stage` equals the
deal's current `stage_code`.** Three of seven cards show days; four show words.
The two absent states are distinguished on the card — `no stage history` versus
`stage entry not recorded` — because they mean different things to whoever
fixes them.

**Live verification (dev Clerk, ephemeral TEST admin, deleted after).** All
seven deals render: Advise 2 (application 1, documents 1), Fund 5 (submitted 1,
lender_response 2, funded 2). Three Fund columns are empty and read
"No files in this stage." with no error styling. Column dollar totals:
advise $853,000, fund $3,588,173. Every network request on the page is a GET;
the server log shows `rec.deals rows=7`, `rec.deal_clients rows=13`,
`rec.deal_stage_events rows=9`, `rec.deal_stages rows=15`, `rec.consents rows=0`
— counts only, never payloads.

**One mockup claim that did not survive first contact, and was fixed rather
than dropped.** The handoff says a board fits one screen with no sideways
scroll. Fund's six columns at a 210px minimum overflowed by 240px at a 1440
viewport with the sidebar open. The minimum is now 168px and the gap 8px
(6 x 168 + 5 x 8 = 1048 against 1080px of board), so the whole phase fits and
the Funded column is visible. Narrower viewports still scroll rather than
crushing a card past readability. **This is width-sensitive**: at 1280 with the
sidebar expanded it will scroll again, and collapsing the rail restores the fit.

**NAV CAP — a standing question for Michael, not resolved here.** The brief
said the nav carries a nine-destination cap and to stop rather than quietly
drop something. Nothing was dropped, but the premise was already one session
stale: A2 added `Tasks` the same morning, taking the working nav to ten, and
this makes **eleven**. The count is asserted in `tests/shell.test.ts` rather
than capped, so the growth stays visible. Deals (Beta) is explicitly temporary
and retires when the record layer ships or is rejected, which returns it to ten.

**Not done, deliberately.** `rec.consents` holds zero rows so Intake has no
source, and Monitor is left to embed the existing Opportunities engine rather
than be rebuilt — both say so on the page. No deal currently carries
`blocked_by = 'you'`, so the lime chip is proven by unit test and by the source
assertion but has never rendered against live data.

### 2026-08-01 — A2: the Tasks page, the Zoho exit's tasks lane

The block spec is `../../fox-underwriting/docs/zoho-exit/blocks/A2-portal-today-page.md`;
the endpoint contract is that repo's `REPORTS/A1.md` and `docs/gates-api.md`
("The native task system"). This repo consumed both as published contracts and
changed nothing in fox-underwriting (guardrail 8, one direction only).

- **Authority keys mirrored, not invented.** `tasks.view`
  (admin/ops/underwriting-reviewer/agent) and `tasks.manage` (admin) added to
  `config/authority.ts`, matching that repo's values verified against its
  `src/gates/tasks.test.ts` rather than assumed. Deliberately SEPARATE from the
  pre-existing `tasks.complete`, which is the LEGACY Zoho card's key and writes
  to Zoho; the two new keys never touch Zoho. One key covering both would make
  `tasks.complete`'s "(writes to Zoho)" label a lie on half its calls.
- **`/portal/admin/tasks`, nav label "Tasks", in the `today` group** beside
  Today. Not named "Today" because that label is taken by `/portal/admin`, and
  two things called Today is how a person ends up on the wrong one.
- **Counts are the server's, always.** Every bucket heading reads
  `counts[bucket]`, never `rows.length`. A1 shipped and fixed the opposite
  defect (counts taken after the 200-row cap, so 276 overdue reported 200);
  `tests/today-tasks.test.ts` pins the distinction with those exact numbers.
- **Paging past the cap, without touching fox-underwriting.** The endpoint caps
  each bucket at 200 and accepts no paging params, so the remaining 76 overdue
  rows are unreachable from it. `tasks` is granted to `portal_readonly`
  (migration 0057), so `getOverdueTasksPage` in `lib/underwriting.ts` reads the
  rest through the existing GET-only wrapper, behind
  `/api/portal/admin/tasks/overdue`. **`as_of` is passed back VERBATIM from the
  endpoint's own response** — the browser never recomputes "today" and neither
  does the route; a missing or malformed `asOf` is refused (422), never
  defaulted, because a defaulted date is a silently different filter. "Show the
  remaining N" REPLACES the overdue list rather than appending: the endpoint
  orders by `due_date` alone, so ties break arbitrarily and appending offset-200
  onto that first page would duplicate some rows and drop others.
- **Bulk triage over the existing endpoints.** Multi-select plus bulk complete
  and bulk dismiss, calling the per-task gate endpoints IN SEQUENCE. No bulk
  endpoint was invented, so every row still lands its own audit entry carrying
  the real human's identity — which is the entire point of guardrail 19.
  Progress counts through; `summarizeBulk`/`bulkSummaryLine` report partial
  failure honestly and count 409 as already-done rather than as an error.
- **Single-tap complete on a row is a deliberate departure** from the approvals
  desk's two-tap confirm, recorded here rather than left to be discovered: this
  is a task list, not a decision queue, and two taps across 276 rows is what
  makes a page get abandoned. Dismiss carries its own friction (a typed reason)
  and the bulk verbs confirm with their count first.
- **Five routes.** `GET /api/portal/admin/tasks/today` and
  `/api/portal/admin/tasks/overdue` behind `tasks.view`; `create`,
  `[taskId]/complete`, `[taskId]/defer`, `[taskId]/dismiss` under
  `/api/portal/admin/gates/tasks/` behind `tasks.manage`. The Zoho-legacy
  `/api/portal/admin/tasks/[id]/status` is untouched and coexists.
- **Demo mode** gets its own fixture (`demoTasksToday`), served at the fetcher
  boundary before any network call — task titles carry client names in the real
  store. The fixture is deliberately truncated (3 rows against a count of 34) so
  a walkthrough exercises the same paging affordance the live page shows.

**Verified live (dev Clerk instance, ephemeral TEST admin, deleted after):**
four buckets with true counts, the "showing 3 of 34" banner and its
"Show the remaining 31" control, action controls and the bulk bar at 375px and
1280px with zero horizontal overflow, the paged-read guards (422 on a missing,
malformed, or injected `asOf`), and the non-admin posture: an `ops` session
reads the page, renders ZERO action controls, and is refused 403 by the SERVER
on both complete and create. Suite 1241 tests (26 new), typecheck clean, build
green.

**FINDING — the dev Clerk instance carries ZERO JWT templates.** Not just no
`gates` template: none at all (`GET /v1/jwt_templates` returns `[]`). So no
gates token can be minted there, and the writes cannot be exercised from local
dev against the production workbench. The page degrades honestly to "Your
session did not produce a token" rather than crashing or showing an empty list.
Production Clerk DOES carry the template (verified on Michael's live session).
Fabricating an identity to close the item locally is what A1 refused and what
guardrail 19 exists to prevent, so it was not done.

**MERGED AND DEPLOYED, same day.** Michael instructed the merge (the "no block
merges itself" rule exists so a human looks first; he is that human). Merge
commit `6de74e1`, Vercel green on it at 18:01:43Z.

### Live on production, against the real store

The page reads real data and the numbers are A1's exactly: **overdue 276, due
today 31, due this week 18, no date 49**, `as_of 2026-08-01`. The overdue bucket
rendered 200 cards under the banner "Showing 200 of 276. The list endpoint caps
a bucket at 200 rows."; "Show the remaining 76" then produced **276 cards with
276 unique ids** — no duplicates, no gaps. That is the paging acceptance item
proven live, and it also confirms the ordering concern the design was built
around (the endpoint orders by `due_date` alone, so appending offset-200 onto
its first page would have duplicated and dropped rows).

### A1's unproven item: THREE of the four verbs are now proven, not four

Michael exercised the verbs himself through the deployed page. Audit log read
back through `portal_readonly`:

| Verb | Audit id | Actor |
|---|---|---|
| `tasks.completed` | `a011023c-d422-4157-8a17-006f50a1f6ee` | `portal` · mfox@foxmortgage.ca · `user_3BamhsuiOxaSkRFj0vDyOTkzSce` |
| `tasks.created` | `fe0b92fc-9573-4169-b5f5-50a73b22cc66` | same |
| `tasks.dismissed` | `41965410-666c-4e65-a1f3-8fb645153fbf` | same |

All three carry `actor='portal'` plus his real Clerk id and email — the identity
record guardrail 19 requires, and the thing A1 refused to fabricate. The
completed row was a real imported task (`zoho_task_id 7112178000006656006`); the
created/dismissed row was the disposable "A2 live proof." task
(`47392071-e83c-4864-8999-c7ef4f59a7c5`), dismissed with a real reason.

**`tasks.deferred` DID NOT LAND, and is still unproven.** Zero
`tasks.deferred` rows exist in `audit_log` ALL TIME, and zero rows anywhere in
`tasks` carry a non-null `deferred_from`. The disposable task's `deferred_from`
is null and its `due_date` (2026-08-03) is the one it was created with. Two
candidate explanations, not yet distinguished: a defer to the date the row
already held is a documented **409 no-op** that writes no audit row, or the
request errored client-side. The plumbing is not in doubt — complete and dismiss
went through the identical `runWrite` path and URL shape and both audited — so
this is about which date was chosen, or an error Michael saw and moved past.

**RELATED UX DEFECT, found while diagnosing this and not yet fixed.** The
client treats a 409 as already-done and drops the row from the list. That is
right for complete and dismiss (409 there means the row IS terminal) but WRONG
for defer, where 409 means "no-op or non-open task" and the row is still open.
The row does return on the refetch that follows, so it self-heals within a round
trip, but for a moment a still-open task reads as decided. Worth fixing by
branching the 409 handling per verb.

**Outstanding:** one defer against an open task, to a date it does not already
hold, to close A1's last verb.

**FINDING — `npm run queue:status` in fox-underwriting could not see this PR.**
`listPullRequests` runs `gh pr list` with `cwd: REPO_ROOT` and no `--repo`
flag, so it only ever sees fox-underwriting pull requests. A2's PR lives here,
so nothing in that repo's derived queue state will reflect it and A2 stays
`ready` there. A2 is a checkpoint so the run ends anyway this time, but a future
cross-repo block would hit the same duplicate-rebuild bug H1b was built to kill.

**Outstanding:** the four gate writes still need one attended run on a session
that can mint a `gates` token — production Clerk, i.e. a preview deploy or a
local run with production keys — to close A1's open acceptance item with real
audit ids.
